rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // For this example, we'll allow anyone to read and write to the lottery_tickets collection.
    // In a real app, you'd want to secure this!
    match /lottery_tickets/{ticketId} {
      allow read, write: if true;
    }
  }
}